#include <stdio.h>
#include <dirent.h>
#include <sys/stat.h>

/**
 * Main
 */
int main(int argc, char **argv)
{
  // Parse command line
  char *dir_name;

  DIR *od;

  struct dirent *enter;
  
  struct stat fileStat;

  // Open directory

  // Repeatly read and print entries

  // Close directory

  return 0;
}